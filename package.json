{
  "name": "aegis-agent-financial-guardrails",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "start": "python3 -m http.server 4173 -d dist",
    "test": "npm run copy:check && node tests/run-tests.js && node tests/final-art-direction.test.js && node tests/black-label.test.js && node tests/red-team-lab.test.js && node tests/ux-scroll.test.js",
    "contract:test": "npm --prefix contracts test",
    "contract:demo": "npm --prefix contracts run demo",
    "contract:parity": "npm --prefix contracts run parity",
    "contract:proof": "npm --prefix contracts run proof",
    "component:audit": "node scripts/audit-components.mjs",
    "copy:snapshot": "node scripts/snapshot-visible-copy.mjs",
    "copy:check": "node scripts/snapshot-visible-copy.mjs --check",
    "test:all": "npm test && npm run component:audit && npm run contract:test && npm run contract:parity"
  },
  "dependencies": {
    "gsap": "3.15.0"
  }
}
